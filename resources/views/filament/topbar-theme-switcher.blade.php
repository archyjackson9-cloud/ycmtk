{{-- Standalone copy of Filament's theme-switcher, surfaced directly in the
     topbar (TOPBAR_START render hook) so light/dark can be toggled without
     opening the user avatar menu. Reuses the same component/logic Filament
     ships under the avatar dropdown - no custom JS. --}}
<x-filament-panels::theme-switcher />
